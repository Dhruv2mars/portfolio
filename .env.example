# AI Activity nightly ingest (Vercel)
# Create a Blob store in the Vercel project, then:
BLOB_READ_WRITE_TOKEN=
AI_ACTIVITY_INGEST_SECRET=
# Set after the first successful ingest (URL returned in the response):
AI_ACTIVITY_BLOB_URL=
